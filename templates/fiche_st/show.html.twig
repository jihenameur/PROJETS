{% extends 'base2.html.twig' %}

{% block title %}FicheST{% endblock %}

{% block body %}
    <h1>FicheST</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fiche_s_t.id }}</td>
            </tr>
            <tr>
                <th>DATE</th>
                <td>{{ fiche_s_t.DATE }}</td>
            </tr>
            <tr>
                <th>Operation</th>
                <td>{{ fiche_s_t.operation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fiche_s_t_index') }}">back to list</a>

    <a href="{{ path('fiche_s_t_edit', {'id': fiche_s_t.id}) }}">edit</a>

    {{ include('fiche_s_t/_delete_form.html.twig') }}
{% endblock %}
